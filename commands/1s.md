---
description: One-sentence replies — short form of /one-sentence
argument-hint: "[question, or 'off' to stop]"
---

Load the `one-sentence` skill now and apply it to this reply and every reply after it, for the rest of the conversation.

Arguments: `$ARGUMENTS`

**If the arguments are empty** (including a bare `?`), this is NOT a question and NOT a request for information about this command:

1. Take your own previous reply — the one that prompted this trigger.
2. Compress it to one sentence under the skill's rules.
3. Send only that sentence. Nothing before it, nothing after it.

Never answer with anything about the skill or the command itself — not that it exists, is available, is installed, is loaded, is active, or is working. The user already knows all of that; that is why they typed the trigger. A reply of the form "yes, /1s is available" or "one-sentence mode is on" is always wrong here. The only correct output is the compressed previous sentence.

If there is no previous reply of yours to compress, say that in one sentence and stop.

**If the arguments are `off`, `stop`, or `end`** — one-sentence mode is finished; acknowledge in one short sentence and answer normally from the next turn.

**Anything else** — answer it under the skill's rules: one sentence, two only if it genuinely has two parts, under 30 words, hard cap 40.
